export const correctnessWhy =
  'Deterministic replay means identical inputs produce identical final state hashes and metrics. This is critical for reproducibility, debugging, and credible low-latency system evaluation.';

export const performanceWhy =
  'Latency percentiles and throughput summarize hot-path behavior. p50 shows common-case cost, while p99 highlights tail risk and jitter relevant to trading infrastructure.';

export const siteProvenance =
  'Each view is built from generated run artifacts and transformed through typed adapters so metrics, charts, and raw files stay synchronized.';
